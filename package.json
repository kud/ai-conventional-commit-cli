{
  "name": "ai-conventional-commit-cli",
  "version": "0.3.9",
  "type": "module",
  "bin": {
    "ai-conventional-commit": "./aicc.mjs",
    "aicc": "./aicc.mjs"
  },
  "description": "Generate Conventional Commits from staged git diff via local AI CLIs (OpenCode default; Codex & Claude optional) with interactive confirm (single model, no fallback)",
  "license": "MIT",
  "dependencies": {
    "zx": "^8.1.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
